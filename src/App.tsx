import * as React from 'react'
import { View, Text } from 'react-native'

export default class App extends React.Component {
  render() {
    return (
      <View>
        <Text>To gets started s, edit and save to reload.</Text>
      </View>
    );
  }
}
